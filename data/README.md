Place the following file here before running ingestion:

- `Placement_RAG_Dataset_Enhanced.pdf` — the source document

The following files are auto-generated by `python scripts/ingest.py`:

- `faiss_index` — FAISS dense vector index
- `bm25_store.pkl` — BM25 sparse index + chunks
- `chunks.pkl` — serialized chunk objects
- `chat_history.json` — persistent UI chat history
- `eval_results.csv` — evaluation output (generated by scripts/evaluate.py)

None of these generated files are committed to git.